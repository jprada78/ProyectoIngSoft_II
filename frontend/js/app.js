// inicialización general.

// Archivo principal que se ejecuta al cargar la aplicación
document.addEventListener("DOMContentLoaded", function() {

    // Se ejecuta cuando todo el HTML ya fue cargado en el navegador
    // Sirve para asegurar que el DOM esté listo antes de ejecutar cualquier lógica
    console.log("Aplicación iniciada correctamente.");
});
